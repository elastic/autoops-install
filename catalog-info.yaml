# Declare a Backstage Component that represents your application.
---
# yaml-language-server: $schema=https://json.schemastore.org/catalog-info.json
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: autoops-install
spec:
  type: tool # or service, library, etc.
  owner: group:opex
  system: autoops
  lifecycle: production
---
# yaml-language-server: $schema=https://gist.githubusercontent.com/elasticmachine/988b80dae436cafea07d9a4a460a011d/raw/rre.schema.json
apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: buildkite-pipeline-autoops-install
  description: Buildkite Pipeline for autoops-install
spec:
  type: buildkite-pipeline
  owner: group:opex
  system: buildkite
  implementation:
    apiVersion: buildkite.elastic.dev/v1
    kind: Pipeline
    metadata:
      name: AutoOps Install CI Pipeline
    spec:
      repository: elastic/autoops-install
      teams:
        opex:
          access_level: MANAGE_BUILD_AND_READ
        everyone:
          access_level: READ_ONLY